console.log(`The maximum safe integer is ${Number.MAX_SAFE_INTEGER}`);
const unsafeNum1 = Number.MAX_SAFE_INTEGER + 1;
const unsafeNum2 = Number.MAX_SAFE_INTEGER + 2;
console.log(`The maximum safe integer plus 1 is ${unsafeNum1}`);
console.log(`The maximum safe integer plus 2 is ${unsafeNum2}`);
